# Local data directory

This directory is reserved for local market data and generated datasets.

The following paths are intentionally ignored by Git:

- `data/raw/`
- `data/processed/`
- `data/features/`

Only schemas, documentation, and small test fixtures should be committed.

