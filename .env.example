# Copy to .env for local-only development.
# Never commit real credentials.

EA_ENV=development
EA_CONFIG_PATH=configs/backtest/example.yaml

# Broker / exchange credentials must stay empty in examples.
BROKER_API_KEY=
BROKER_API_SECRET=

