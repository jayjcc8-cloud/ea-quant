# Process-environment example for local development.
# This file is documentation only; EA does not load .env files implicitly.

EA_ENVIRONMENT=development
EA_RUN_MODE=backtest
EA_CONFIG_PATH=configs/backtest/example.yaml

# Raw credentials are not application configuration.
# Do not add broker/exchange keys, tokens, or secrets to EA_* settings.
