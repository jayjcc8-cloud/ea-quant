# Issue #13 defines only the outer composition settings that have stable contracts.
# Data, strategy, risk, manifest, and execution schemas belong to later Issues.
schema_version: 1
environment: development
run:
  mode: backtest
